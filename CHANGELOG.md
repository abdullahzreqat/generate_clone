## 1.0.4

- Auto-detect available platforms (android/ios) for rename command
- Ensure temp directory cleanup always runs (even on errors)

## 1.0.3

- Fix pubspec.yaml asset path insertion breaking YAML structure

## 1.0.2

- Add dynamic fields support with auto-type detection
- Add sensitive fields support using `$ENV:` syntax with flexible resolver pattern
- Add flexible input sources: local directory, ZIP file, or remote URL
- Add auto project configuration (pubspec.yaml, flutter_launcher_icons.yaml, flutter_native_splash.yaml)
- Fetch latest package versions from pub.dev API
- Add complete example for pub.dev

## 1.0.1

- Handle files from directory input

## 1.0.0

- Initial version.
